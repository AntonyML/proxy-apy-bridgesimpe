{
  "name": "proxy-apy-bridgesimpe",
  "version": "1.0.0",
  "description": "Industrial-grade proxy middleware for SINPE Bridge API",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "secret:set": "wrangler secret put API_KEY",
    "secret:list": "wrangler secret list",
    "logs": "wrangler tail --env production"
  },
  "dependencies": {
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "wrangler": "^4.93.0"
  }
}
